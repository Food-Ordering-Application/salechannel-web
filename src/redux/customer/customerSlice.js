import {createSlice} from "@reduxjs/toolkit";

export const customerSlice = createSlice({
  name: `customer`,
  initialState: {},
  reducers: {
    login: state => {

    },
    logout: state => {

    }
  },
});